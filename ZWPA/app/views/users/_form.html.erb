<h4> Create User <h4>

<div class="container">
    <div class="left-align">
        <div class="row">
            <div class="col s6">
                <div class="row">
                    <%= simple_form_for @client do |f| %>
                    
                        <br>
                        <div class="col s6">

                          <%= f.input :email, wrapper_html: { class: 'col s12' } %>
                          <%= f.input :password_digest, wrapper_html: { class: 'col s12' } %>
                        </div>
                            
                        <div class="col s12">
                            <div class="btn waves-effect waves-light"> Back</a>
                            <div class="btn waves-effect waves-light" >
                              <%= f.submit "Submit" %>
                         </div>
                        </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>
