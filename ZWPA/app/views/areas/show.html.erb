
<div class="row">
    <div class="col s12">
        <ul class="tabs">
            <li class="tab col s2"><a href="#c1" class="teal-text text-darken-2">Landfill</a></li>
            <li class="tab col s2"><a href="#c2" class="teal-text text-darken-2">Recycle</a></li>
            <li class="tab col s2"><a href="#c3" class="teal-text text-darken-2">Compost</a></li>
            <li class="tab col s2"><a href="#c4" class="teal-text text-darken-2">Reuse</a></li>
            <li class="tab col s2"><a href="#c5" class="teal-text text-darken-2">Food Recovery</a></li>
        </ul>
    </div>
</div>
<div class="row">
    <div id="c1" class="col s12">
            <table id = "landfill" class="display">
                <thead>
                    <tr>
                        <th>Category</th>
                        <th>Weight</th>
                        <th>Notes</th>
                        <th>Anomaly</th>
                        <th>True Category</th>
                    </tr>
                </thead>

                <tbody>
                    <% @landfill do |landfill| %>
                        <tr>
                            <td><%= landfill.category %></td>
                            <td><%= landfill.name %></td>
                            <td><%= landfill.notes %></td>
                            <td><%= landfill.true_category %></td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
    </div>
    <div id="c2" class="col s12">
            <table id = "recycle" class="display">
                <thead>
                    <tr>
                        <th>Category</th>
                        <th>Weight</th>
                        <th>Notes</th>
                        <th>Anomaly</th>
                        <th>True Category</th>
                    </tr>
                </thead>

                <tbody>
                    <% @recycling.each do |recycle| %>
                        <tr>
                            <td><%= recycle.category %></td>
                            <td><%= recycle.name %></td>
                            <td><%= recycle.notes %></td>
                            <td><%= recycle.true_category %></td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
    </div>
    <div id="c3" class="col s12">
            <table id = "compost" class="display">
                <thead>
                    <tr>
                        <th>Category</th>
                        <th>Weight</th>
                        <th>Notes</th>
                        <th>Anomaly</th>
                        <th>True Category</th>
                    </tr>
                </thead>

                <tbody>
                    <% @compost.each do |compost| %>
                        <tr>
                            <td><%= compost.category %></td>
                            <td><%= compost.name %></td>
                            <td><%= compost.notes %></td>
                            <td><%= compost.true_category %></td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
    </div>
    <div id="c4" class="col s12">
            <table id = "reuse" class="display">
                <thead>
                    <tr>
                        <th>Category</th>
                        <th>Weight</th>
                        <th>Notes</th>
                        <th>Anomaly</th>
                        <th>True Category</th>
                    </tr>
                </thead>

                <tbody>
                    <% @reuse.each do |reuse| %>
                        <tr>
                            <td><%= reuse.category %></td>
                            <td><%= reuse.name %></td>
                            <td><%= reuse.notes %></td>
                            <td><%= reuse.true_category %></td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
    </div>
    <div id="c5" class="col s12">
            <table id = "food_recovery" class="display">
                <thead>
                    <tr>
                        <th>Category</th>
                        <th>Weight</th>
                        <th>Notes</th>
                        <th>Anomaly</th>
                        <th>True Category</th>
                    </tr>
                </thead>

                <tbody>
                    <% @food_recovery.each do |food| %>
                        <tr>
                            <td><%= food.category %></td>
                            <td><%= food.name %></td>
                            <td><%= food.notes %></td>
                            <td><%= food.true_category %></td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
    </div>
</div>


<%= link_to 'Back', areas_path %>
